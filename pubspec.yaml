name: in_app_mcp
description: "Policy-gated tool runtime for in-app LLM/agent tools. Per-tool auto/confirm/deny, typed contracts, structured results."
version: 1.1.0
homepage: https://github.com/phanbaohuy96/in-app-mcp
repository: https://github.com/phanbaohuy96/in-app-mcp
issue_tracker: https://github.com/phanbaohuy96/in-app-mcp/issues

environment:
  sdk: ^3.10.8
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
